node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# api/ is deployed as raw TS (Vercel builds it itself) — never commit compiled output here
/api/*.js
/api/*.js.map
/api/*.d.ts
